import 'package:flutter/material.dart';

class Winehouses extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wine Houses'),
        backgroundColor: Colors.pink[900],
      ),
    );
  }
}
